package openvpn

// Down generates the Down rules
func (c *ServerConfig) PostDown() (rules []string) {
	return rules
}

// Up generates the Up rules
func (c *ServerConfig) PostUp() (rules []string) {
	return rules
}
